# Frontend Web

Este app faz parte do monorepo `sitecompras`.

Para instalacao, ambiente e execucao local, use o guia da raiz do projeto:

- `../../README.md`

O arquivo `.env` desta app e gerado automaticamente por:

```bash
npm run setup:env
```
